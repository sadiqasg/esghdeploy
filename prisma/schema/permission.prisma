model Permission {
  id                Int @id @default(autoincrement())
  can_add_user      Boolean @default(false)
  can_add_department Boolean @default(false)
  can_input_data    Boolean @default(false)
  can_generate_report Boolean @default(false)

  users User[]

  @@map("permissions")
}
